import { StyleSheet, Text, View } from "react-native";
import React from "react";

const CartReducer = () => {
    return (
        <View>
            <Text>CartReducer</Text>
        </View>
    );
};

export default CartReducer;

const styles = StyleSheet.create({});
